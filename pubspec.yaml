name: pubghost
description: A sample CLI tool to find unused packages in a Flutter project.
version: 1.0.0

environment:
  sdk: ^3.9.2

dependencies:
  path: ^1.9.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6

executables:
  pubghost: pubghost
